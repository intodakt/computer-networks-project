Contribution

Member 1: [Name] - Server Architecture

Implemented the handle_client threading loop in whiteboard_server.py.

Implemented the threading.Lock logic to prevent race conditions.

Designed the "History Replay" feature so new users see previous drawings.

Member 2: [Name] - GUI Layout & Sandwich Design

Designed the Tkinter GUI layout in whiteboard_client.py.

Implemented the "Sandwich" packing strategy to ensure the Chat Entry field is always visible and adaptable to window resizing.

Integrated the PanedWindow for adjustable sidebars.

Member 3: [Name] - Network Protocol & Documentation

Designed the WBTP (Whiteboard Transfer Protocol) specifications found in README.md.

Wrote the Makefile for easy execution.

Implemented the receive_messages network thread in the client to prevent GUI freezing.

Member 4: [Name] - Drawing Logic & Interpolation

Implemented the logic for Shapes (Rectangle, Circle, Triangle) in on_drag and on_release.

Solved the "Dotted Line" issue by implementing coordinate interpolation (drawing lines between mouse events instead of just points).

Handled the specific canvas.create_line arguments for smooth caps.